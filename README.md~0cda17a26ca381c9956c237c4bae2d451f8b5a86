# breastfeeding_resourcemap

cd into next-app then run "yarn dev" to run things. This launches the site on http://localhost:3000/.

To see the Airtable data on the site, you will need to add the API key as 'NEXT_PUBLIC_AIRTABLE_API_KEY' in a .env file in next-app.
